import 'package:flutter/material.dart';

class NinjaColors {
  // Backgrounds
  static const bgPrimary = Color(0xFF0F0F0F);
  static const bgSecondary = Color(0xFF141414);
  static const bgElevated = Color(0xFF1A1A1A);

  // Metal surfaces
  static const metalLight = Color(0xFF3A3A3A);
  static const metalMid = Color(0xFF2A2A2A);
  static const metalDark = Color(0xFF1F1F1F);

  // Borders / edges
  static const metalEdgeSoft = Color(0xFF404040);
  static const metalEdgeHard = Color(0xFF5A5A5A);

  // Text
  static const textPrimary = Color(0xFFE6E6E6);
  static const textSecondary = Color(0xFFB0B0B0);
  static const textMuted = Color(0xFF7A7A7A);

  // Accent (energy / focus)
  static const accent = Color(0xFF8AFFC1);

  // States
  static const success = Color(0xFF7CFF9B);
  static const warning = Color(0xFFFFC46B);
  static const error = Color(0xFFFF6B6B);
}

